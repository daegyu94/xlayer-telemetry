# Copy to a private local path and edit these two values.
# This is Bash syntax; quote each VERL argument that contains spaces.
RUN_ID='grpo-001'
VERL_COMMAND=(
  /path/to/verl-env/bin/python -m verl.trainer.main_ppo
  # Add the model, dataset, batch, and GPU settings from your working recipe.
)

# Optional local single-host settings:
# TELEMETRY_HOME="$HOME/telemetry"
# RUN_ROOT="$HOME/telemetry-runs/$RUN_ID"
# NODE_NAME='gpu-local'
# ENABLE_LOGS=1
# ENABLE_ALERTS=1
# TELEMETRY_SOURCES_FILE="$HOME/telemetry/config/native-sources.json"
# DIAGNOSTICS_CONFIG="$HOME/telemetry/config/diagnostics.json"
